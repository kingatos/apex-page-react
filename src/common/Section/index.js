import { Wrapper, SectionBody } from "./styled";

const Section = ({ body }) => {
    <Wrapper>
        <SectionBody>
            {body}
        </SectionBody>
    </Wrapper>
}

export default Section;